name: Build Launcher-NX

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    container: devkitpro/devkita64:latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Install Missing Switch SDK Libraries
        run: |
          # Force sync and install the entire native Switch development ecosystem inside the cloud runner
          dkp-pacman -Syy --noconfirm
          dkp-pacman -S switch-dev --noconfirm

      - name: Build via Switch Toolchain
        run: |
          # Use the official environment compiler directly to handle all relocation layout math safely
          export PATH=$DEVKITPRO/devkitA64/bin:$PATH
          
          # 1. Compile source with proper platform configurations
          aarch64-none-elf-g++ -O2 -ffunction-sections -fdata-sections -fPIE -march=armv8-a+crc+crypto -mtune=cortex-a57 -I$DEVKITPRO/libnx/include -c main.cpp -o main.o
          
          # 2. Link with correct specs and flags to clear the dynamic relocations error completely
          aarch64-none-elf-g++ -specs=$DEVKITPRO/libnx/switch.specs -g -march=armv8-a+crc+crypto -mtune=cortex-a57 -Wl,-Map,Launcher-NX.map -ftl-optimization -fPIE -pie main.o -L$DEVKITPRO/libnx/lib -lnx -o Launcher-NX.elf
          
          # 3. Create the final clean Switch executable format
          elf2nro Launcher-NX.elf Launcher-NX.nro --title="Launcher-NX" --author="Homebrew Dev" --version="1.0.0"

      - name: Upload Launcher-NX
        uses: actions/upload-artifact@v4
        with:
          name: Launcher-NX
          path: ./Launcher-NX.nro
